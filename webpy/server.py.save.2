#!/usr/bin/python
import web

urls = (
     '/', 'index'
)

class index:
	def GET(self):
		for line in data:
			linereturn data.readlines()

data = open("voltage.txt","r")

if __name__ == "__main__":
	app = web.application(urls, globals())
	app.run()
