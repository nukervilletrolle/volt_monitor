x#!/usr/bin/python
import web

urls = (
     '/', 'index'
)

class index:
	def GET(self):
		for line in data:
			x = x + 1
		return data.readline(x-1)

data = open("voltage.txt","r")

if __name__ == "__main__":
	app = web.application(urls, globals())
	app.run()
